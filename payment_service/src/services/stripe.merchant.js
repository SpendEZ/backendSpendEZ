import { createConnectedStripeAccount } from "../gateways/stripe/merchant.stripe";

// use this to match a connected stripe account with id of merchant in database
//the create merchant account returns everything u will need
